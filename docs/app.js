const routes={home:'Home',installation:'Installation-and-Setup',configuration:'Configuration','multi-controller':'Multi-Controller',entities:'Entities',dashboards:'Dashboards',sensors:'Sensors',switches:'Switches',climate:'Climate',states:'Device-States',services:'Services',automations:'Automations',troubleshooting:'Troubleshooting',diagnostics:'Diagnostics',logging:'Erweiterte-Protokollierung','error-codes':'Error-Codes',security:'Security',faq:'FAQ',api:'API-Package',contributing:'Contributing',changelog:'Changelog'};
const titles={home:'Übersicht',installation:'Installation & Einrichtung',configuration:'Konfiguration','multi-controller':'Mehrere Controller',entities:'Alle Entitäten',dashboards:'Dashboards & Pool-Karten',sensors:'Sensoren & Messwerte',switches:'Schalter & Steuerung',climate:'Klima & Heizung',states:'Gerätezustände',services:'Services',automations:'Automatisierungen',troubleshooting:'Fehlerbehebung',diagnostics:'Diagnosedaten',logging:'Protokollierung','error-codes':'Fehlercodes',security:'Sicherheit & SSL',faq:'FAQ',api:'Python API',contributing:'Mitwirken',changelog:'Changelog'};
const content=document.querySelector('#content'),crumb=document.querySelector('.breadcrumbs b'),aside=document.querySelector('aside'),overlay=document.querySelector('.overlay');
function route(){return location.hash.replace(/^#\/?/,'').split('/')[0]||'home'}
function normalizeLinks(){content.querySelectorAll('a[href]').forEach(a=>{const href=a.getAttribute('href');if(!href||/^(https?:|mailto:|#)/.test(href))return;const base=href.replace(/\.de(?:\.md)?$|\.md$/,'').split('#')[0];const key=Object.keys(routes).find(k=>routes[k].toLowerCase()===base.toLowerCase());if(key)a.href='#/'+key})}
async function load(){const key=route(),page=routes[key]||routes.home;crumb.textContent=titles[key]||titles.home;document.title=`${crumb.textContent} · Violet Dokumentation`;document.querySelectorAll('aside a[data-page]').forEach(a=>a.classList.toggle('active',a.dataset.page===page));content.innerHTML='<div class="loading"><i></i><span>Dokumentation wird geladen …</span></div>';try{let response=await fetch(`wiki/${page}.de.md`);if(!response.ok)response=await fetch(`wiki/${page}.md`);if(!response.ok)throw new Error();let md=await response.text();md=md.replace(/^﻿/,'').replace(/^> 🇩🇪[^\n]*\n?/m,'').replace(/^> 🇬🇧[^\n]*\n?/m,'');marked.setOptions({gfm:true,breaks:false});content.innerHTML=DOMPurify.sanitize(marked.parse(md),{ADD_ATTR:['target']});normalizeLinks();content.querySelectorAll('pre').forEach(pre=>{const btn=document.createElement('button');btn.className='copy';btn.textContent='Kopieren';btn.onclick=async()=>{await navigator.clipboard.writeText(pre.innerText);btn.textContent='Kopiert ✓';setTimeout(()=>btn.textContent='Kopieren',1600)};pre.append(btn)});window.scrollTo({top:0,behavior:'instant'})}catch{content.innerHTML='<div class="error"><b>Diese Seite konnte nicht geladen werden.</b><a href="https://github.com/Xerolux/violet-hass/wiki">GitHub Wiki öffnen →</a></div>'}closeMenu()}
function closeMenu(){aside.classList.remove('open');overlay.classList.remove('show')};document.querySelector('.menu').onclick=()=>{aside.classList.toggle('open');overlay.classList.toggle('show')};overlay.onclick=closeMenu;window.addEventListener('hashchange',load);document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='k'){e.preventDefault();document.querySelector('.search input').focus()}});document.querySelector('.search input').addEventListener('input',e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('aside nav a').forEach(a=>a.hidden=q&&!a.textContent.toLowerCase().includes(q))});load();
