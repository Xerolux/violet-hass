# =============================================================================
# Violet Pool Controller - ULTIMATE CONTROL DASHBOARD
# =============================================================================
#
# This dashboard exposes EVERY control option for each device
# in compact, readable cards.
#
# BEST FOR: full control, every setting at a glance
#
# NOTE: 
# - Requires custom cards from HACS (Mushroom, Slider Entity Row, Card Mod)
# - Replace "violet_pool_controller" with your own device name
#
# VERSION: 1.0.5 (2026-04-22)
# =============================================================================

type: vertical-stack
cards:
  # ============================================================================
  # 🏊 FILTER PUMP - full control
  # ============================================================================
  - type: custom:mushroom-template-card
    primary: Filter pump
    secondary: |
      {% if is_state('sensor.violet_pool_controller_pump_status', 'Frostschutz') %}
        ❄️ Frost protection active
      {% elif is_state('switch.violet_pool_controller_pump', 'on') %}
        ✅ Running ({{ states('number.violet_pool_controller_pump_speed') }}/3)
      {% else %}
        🔴 Off
      {% endif %}
    icon: mdi:water-pump
    icon_color: |
      {% if is_state('sensor.violet_pool_controller_pump_status', 'Frostschutz') %}
        cyan
      {% elif is_state('switch.violet_pool_controller_pump', 'on') %}
        green
      {% else %}
        grey
      {% endif %}
    tap_action:
      action: more-info
      entity: switch.violet_pool_controller_pump

  - type: entities
    show_header_toggle: false
    state_color: true
    card_mod:
      style: |
        ha-card {
          margin-top: -16px;
          border-top: none;
        }
    entities:
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_pump
        name: Mode
        icon: mdi:auto-mode
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_pump_speed
        name: Speed
        icon: mdi:speedometer
        min: 1
        max: 3
        step: 1
      - entity: sensor.violet_pool_controller_pump_status
        name: Status
        icon: mdi:information
      - entity: sensor.violet_pool_controller_pump_runtime
        name: Runtime
        icon: mdi:timer-outline

  # ============================================================================
  # 🔥 HEATER - full control
  # ============================================================================
  - type: custom:mushroom-template-card
    primary: Heater
    secondary: |
      {% set temp = states('sensor.violet_pool_controller_pool_temperature') | float(0) %}
      {% set target = states('number.violet_pool_controller_heater_target_temp') | float(0) %}
      {{ temp }}°C → {{ target }}°C
    icon: mdi:radiator
    icon_color: |
      {% if is_state('switch.violet_pool_controller_heater', 'on') %}
        red
      {% else %}
        grey
      {% endif %}
    tap_action:
      action: more-info
      entity: climate.violet_pool_controller_heater

  - type: entities
    show_header_toggle: false
    state_color: true
    card_mod:
      style: |
        ha-card {
          margin-top: -16px;
          border-top: none;
        }
    entities:
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_heater
        name: Mode
        icon: mdi:auto-mode
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_heater_target_temp
        name: Target temperature
        icon: mdi:thermometer-chevron-up
        min: 20
        max: 35
        step: 0.5
      - entity: sensor.violet_pool_controller_heater_status
        name: Status
        icon: mdi:information
      - entity: sensor.violet_pool_controller_heater_runtime
        name: Runtime
        icon: mdi:timer-outline

  # ============================================================================
  # ☀️ SOLAR - full control
  # ============================================================================
  - type: custom:mushroom-template-card
    primary: Solar
    secondary: |
      {% set temp = states('sensor.violet_pool_controller_pool_temperature') | float(0) %}
      {% set target = states('number.violet_pool_controller_solar_target_temp') | float(0) %}
      {{ temp }}°C → {{ target }}°C
    icon: mdi:solar-power
    icon_color: |
      {% if is_state('switch.violet_pool_controller_solar', 'on') %}
        orange
      {% else %}
        grey
      {% endif %}
    tap_action:
      action: more-info
      entity: climate.violet_pool_controller_solar

  - type: entities
    show_header_toggle: false
    state_color: true
    card_mod:
      style: |
        ha-card {
          margin-top: -16px;
          border-top: none;
        }
    entities:
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_solar
        name: Mode
        icon: mdi:auto-mode
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_solar_target_temp
        name: Target temperature
        icon: mdi:thermometer-chevron-up
        min: 20
        max: 40
        step: 0.5
      - entity: sensor.violet_pool_controller_solar_status
        name: Status
        icon: mdi:information
      - entity: sensor.violet_pool_controller_solar_runtime
        name: Runtime
        icon: mdi:timer-outline

  # ============================================================================
  # 💧 pH CONTROL - full control
  # ============================================================================
  - type: custom:mushroom-template-card
    primary: pH control
    secondary: |
      {% set ph = states('sensor.violet_pool_controller_ph_value') | float(0) %}
      {% set target = states('number.violet_pool_controller_ph_setpoint') | float(7.2) %}
      Ist: {{ ph }} | Soll: {{ target }}
    icon: mdi:flask
    icon_color: |
      {% set ph = states('sensor.violet_pool_controller_ph_value') | float(0) %}
      {% if ph < 7.0 %}
        red
      {% elif ph > 7.6 %}
        orange
      {% else %}
        green
      {% endif %}

  - type: entities
    show_header_toggle: false
    state_color: true
    card_mod:
      style: |
        ha-card {
          margin-top: -16px;
          border-top: none;
        }
    entities:
      - type: section
        label: pH minus
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_dos_4_phm
        name: pH- mode
        icon: mdi:minus-circle
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_ph_minus_canister_volume
        name: Canister (ml)
        icon: mdi:barrel
        min: 1000
        max: 50000
        step: 1000
      - entity: sensor.violet_pool_controller_dos_4_phm_remaining_range
        name: Range
        icon: mdi:calendar-clock
      - entity: sensor.violet_pool_controller_dos_4_phm_runtime
        name: Runtime
        icon: mdi:timer-outline

      - type: section
        label: pH plus
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_dos_5_php
        name: pH+ mode
        icon: mdi:plus-circle
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_ph_plus_canister_volume
        name: Canister (ml)
        icon: mdi:barrel
        min: 1000
        max: 50000
        step: 1000
      - entity: sensor.violet_pool_controller_dos_5_php_remaining_range
        name: Range
        icon: mdi:calendar-clock
      - entity: sensor.violet_pool_controller_dos_5_php_runtime
        name: Runtime
        icon: mdi:timer-outline

      - type: section
        label: Setpoint
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_ph_setpoint
        name: pH Target
        icon: mdi:target
        min: 6.8
        max: 7.8
        step: 0.1

  # ============================================================================
  # 🟢 CHLORINE CONTROL - full control
  # ============================================================================
  - type: custom:mushroom-template-card
    primary: Chlorine control
    secondary: |
      {% set cl = states('sensor.violet_pool_controller_chlorine_level') | float(0) %}
      {% set target = states('number.violet_pool_controller_chlorine_setpoint') | float(0.6) %}
      Ist: {{ cl }} mg/l | Soll: {{ target }} mg/l
    icon: mdi:test-tube
    icon_color: |
      {% set cl = states('sensor.violet_pool_controller_chlorine_level') | float(0) %}
      {% if cl < 0.3 %}
        red
      {% elif cl > 1.5 %}
        orange
      {% else %}
        green
      {% endif %}

  - type: entities
    show_header_toggle: false
    state_color: true
    card_mod:
      style: |
        ha-card {
          margin-top: -16px;
          border-top: none;
        }
    entities:
      - type: custom:mushroom-select-card
        entity: switch.violet_pool_controller_dos_1_cl
        name: Chlorine mode
        icon: mdi:auto-mode
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_chlorine_canister_volume
        name: Canister (ml)
        icon: mdi:barrel
        min: 1000
        max: 50000
        step: 1000
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_chlorine_setpoint
        name: Setpoint
        icon: mdi:target
        min: 0.2
        max: 2.0
        step: 0.1
      - type: custom:slider-entity-row
        entity: number.violet_pool_controller_orp_setpoint
        name: ORP Setpoint
        icon: mdi:flash
        min: 600
        max: 800
        step: 10
      - entity: sensor.violet_pool_controller_orp_value
        name: ORP now
        icon: mdi:flash-outline
      - entity: sensor.violet_pool_controller_dos_1_cl_remaining_range
        name: Range
        icon: mdi:calendar-clock
      - entity: sensor.violet_pool_controller_dos_1_cl_runtime
        name: Runtime
        icon: mdi:timer-outline

# =============================================================================
# NOTE: this dashboard needs the following custom cards (via HACS):
#   - mushroom (https://github.com/piitaya/lovelace-mushroom)
#   - slider-entity-row (https://github.com/thomasloven/lovelace-slider-entity-row)
#   - card-mod (https://github.com/thomasloven/lovelace-card-mod)
#
# Installation:
# 1. HACS -> Frontend -> search for "Mushroom", "Slider Entity Row", "Card Mod"
# 2. Install them and restart Home Assistant
# 3. Dashboard -> Edit -> Add card -> Manual
# 4. Paste this YAML
# =============================================================================
